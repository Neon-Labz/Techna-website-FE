import { Suspense } from 'react';
import ResetPasswordSection from '@/components/website/ResetPasswordSection';

export default function ResetPasswordPage() {
  return (
    <Suspense>
      <ResetPasswordSection />
    </Suspense>
  );
}
