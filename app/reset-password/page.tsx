import { Suspense } from 'react';
import ResetPasswordSection from '../../src/components/website/ResetPasswordSection';

export default function ResetPasswordPage() {
  return (
    <Suspense>
      <ResetPasswordSection />
    </Suspense>
  );
}
